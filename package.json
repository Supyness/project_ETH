{
  "name": "color-grid-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "hardhat test",
    "lint": "hardhat check",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/node": "^20.11.0",
    "dotenv": "^16.4.0",
    "hardhat": "^2.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
